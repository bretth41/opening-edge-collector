[build]
builder = "NIXPACKS"
[deploy]
startCommand = "sh -c 'python collector.py & uvicorn research_app:app --host 0.0.0.0 --port ${PORT:-8080}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
